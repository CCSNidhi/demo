name: .NET

on: 
  push: 
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ] 

jobs: 
  build:
    runs-on: Windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2
      - name: Setup .NET
        uses: actions/setup-dotnet@v2
        with:
          dotnet-version: 7.0.x
      - name: Install MAUI workload 
        run: dotnet workload install maui
        working-directory: ./MauiApp_version/MauiApp_version
      
      - name: Determine Version
        id: determine_version
        run: |
          $VERSION=$(git describe --abbrev=0 --tags)
          $LAST_COMMIT_MESSAGE=$(git log -1 --pretty=format:%s)
          if ($LAST_COMMIT_MESSAGE -match "\[major\]") {
            $NEW_VERSION="1.0.0"  # Adjust the version calculation as needed
          } elseif ($LAST_COMMIT_MESSAGE -match "\[minor\]") {
            $NEW_VERSION="0.1.0"  # Adjust the version calculation as needed
          } else {
            $NEW_VERSION="0.0.1"  # Adjust the version calculation as needed
          }
          echo "New version: $NEW_VERSION"
          echo "##[set-output name=new_version]$NEW_VERSION"


      - name: Build
        run: dotnet publish MauiApp_version.csproj -c Release -f:net7.0-windows10.0.19041.0 /p:GenerateAppxPackageOnBuild=true 
        working-directory: ./MauiApp_version/MauiApp_version

      - name: Upload Build Artifacts
        uses: actions/upload-artifact@v2
        with:
          name: maui-app-msix
          path: ./**/*.msix
          






        




